apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pixeltable-starter.fullname" . }}
  labels:
    {{- include "pixeltable-starter.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
