apiVersion: v1
kind: Service
metadata:
  name: {{ include "pixeltable-starter.fullname" . }}
  labels:
    {{- include "pixeltable-starter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
  selector:
    {{- include "pixeltable-starter.selectorLabels" . | nindent 4 }}
